const search = Object.freeze({
    types: { 
        NUMERIC: 'numeric', 
        STRING: 'search' },
    placeholder: { 
        NUMERIC: 'Digite o numero:', 
        STRING: 'Digite o nome ou um trecho:' },
})

export default search